 <Window x:Class="Names.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Names"
        mc:Ignorable="d"
        Title="Names" Height="180" Width="260"
         Closing="Window_Closing"
         Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Label>Names</Label>
        <ListBox Grid.Row="1" x:Name="lstNames" SelectedItem="lstNames_SelectedIndexChanged" KeyDown="deleteNames_KeyDown"/>
        <StackPanel Grid.Row="1" Grid.Column="1" Margin="5,0,0,0">
            <TextBox x:Name="inputNames" KeyDown="inputNames_KeyDown"></TextBox>
            <Button x:Name="addNames" Margin="0,5,0,0" Click="addNames_Click">Add Name</Button>
            <Button x:Name="deleteAll" Margin="0,5,0,0" Click="deleteNames_Click">Delete</Button>
        </StackPanel>
    </Grid>
</Window>
